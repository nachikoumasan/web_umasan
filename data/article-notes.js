// 記事ごとの手動補足。noteから取れる内容をサイト側で変えたいときだけ使う。
// キーは記事URL。title でサイト用タイトル、description で紹介文を上書きできる。
// 空のときは、すべてnoteの内容（タイトル・本文の1文目）がそのまま出ます。
// 運用は「空のまま」が既定。個別に直したい記事が出たときだけ1件足してください。
window.UMASAN_ARTICLE_NOTES = {
};
